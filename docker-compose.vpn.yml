# Alternativa si WireGuard corre en el HOST (no en contenedor wg-easy).
# Expone puerto 8000 en 10.8.0.1 además de localhost.
#
# Uso:
#   docker compose -f docker-compose.standalone.yml -f docker-compose.vpn.yml up -d --build

services:
  backend:
    ports:
      - "10.8.0.1:8000:8000"